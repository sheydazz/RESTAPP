import 'package:flutter/material.dart';

class AdviceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Pantalla de consejos de emociones en caso necesite")),
    );
  }
}
