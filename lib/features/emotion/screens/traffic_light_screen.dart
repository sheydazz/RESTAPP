import 'package:flutter/material.dart';

class TrafficLightScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Pantalla donde se muestra el estado de las emocion segun la conversacion")),
    );
  }
}
