import 'package:flutter/material.dart';

class EmotionRegisterScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Pantalla de registro de emociones desde la tab")),
    );
  }
}
