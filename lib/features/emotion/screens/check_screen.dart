import 'package:flutter/material.dart';

class CheckScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Pantalla de registro de en la semana")),
    );
  }
}
